# دليل استخدام قالب استيراد الأماكن

## 📋 الأعمدة المطلوبة (Required Columns)

يجب أن يحتوي ملف Excel أو CSV على الأعمدة التالية:

### أعمدة إلزامية (Required):
- **name** (الاسم): اسم المكان بالعربية
- **description** (الوصف): وصف تفصيلي للمكان
- **type** (النوع): نوع المكان - يجب أن يكون واحد من:
  - `religious` (ديني)
  - `historical` (تاريخي)
  - `entertainment` (ترفيهي)
- **latitude** (خط العرض): الإحداثيات الجغرافية (مثال: 24.4672)
- **longitude** (خط الطول): الإحداثيات الجغرافية (مثال: 39.6111)

### أعمدة اختيارية (Optional):
- **audience** (الفئة): الفئات المناسبة - يمكن كتابتها كقائمة مفصولة بفواصل:
  - `family` (عائلي)
  - `kids` (أطفال)
  - `seniors` (كبار سن)
  - `friends` (أصدقاء)
  - مثال: `family,kids` أو `["family","kids"]`
  
- **environment** (البيئة): نوع البيئة:
  - `indoor` (داخلي)
  - `outdoor` (خارجي)
  - `mixed` (مختلط)
  
- **requiresBooking** (يتطلب حجز): `true` أو `false` أو `1` أو `0`
- **reservationPrice** (سعر الحجز): رقم (مثال: 50)
- **bookingUrl** (رابط الحجز): رابط خارجي للحجز
- **bookingsCount** (عدد الحجوزات): رقم (مثال: 1000)
- **openingHours** (ساعات العمل): نص (مثال: "24 ساعة" أو "9 صباحاً - 10 مساءً")
- **crowdLevel** (مستوى الازدحام):
  - `low` (منخفض)
  - `medium` (متوسط)
  - `high` (عالي)
  
- **currentEvents** (الأحداث الحالية): قائمة مفصولة بفواصل (مثال: `حدث1,حدث2`)
- **images** (الصور): روابط الصور مفصولة بفواصل (مثال: `https://example.com/img1.jpg,https://example.com/img2.jpg`)
- **customFilters** (فلاتر مخصصة): يمكن تركه فارغاً

## 📝 أمثلة على القيم

### النوع (type):
- `religious` = ديني
- `historical` = تاريخي
- `entertainment` = ترفيهي

### الفئة (audience):
- `family` = عائلي
- `kids` = أطفال
- `seniors` = كبار سن
- `friends` = أصدقاء
- يمكن كتابة أكثر من فئة: `family,kids` أو `["family","kids"]`

### البيئة (environment):
- `indoor` = داخلي
- `outdoor` = خارجي
- `mixed` = مختلط

### مستوى الازدحام (crowdLevel):
- `low` = منخفض
- `medium` = متوسط
- `high` = عالي

## ⚠️ ملاحظات مهمة

1. **الأعمدة يجب أن تكون باللغة الإنجليزية** (name, description, type, etc.)
2. **القيم يمكن أن تكون بالعربية** (الأسماء، الأوصاف، إلخ)
3. **الإحداثيات** يجب أن تكون أرقام صحيحة (latitude, longitude)
4. **القوائم** (audience, images, events) يمكن كتابتها:
   - مفصولة بفواصل: `family,kids`
   - أو بصيغة JSON: `["family","kids"]`
5. **القيم الفارغة** يمكن تركها فارغة أو كتابة `false` أو `0` للأرقام

## 📤 كيفية الاستيراد

1. قم بتحميل القالب من صفحة `/admin`
2. افتحه في Excel أو Google Sheets
3. املأ البيانات حسب القالب
4. احفظ الملف كـ Excel (.xlsx) أو CSV (.csv)
5. ارفع الملف من صفحة `/admin`
6. انتظر حتى يكتمل الاستيراد

## ✅ مثال كامل

```
name: المسجد النبوي
description: أحد أعظم المساجد في الإسلام
type: religious
audience: family,seniors,friends
environment: mixed
requiresBooking: false
latitude: 24.4672
longitude: 39.6111
openingHours: 24 ساعة
crowdLevel: high
```

